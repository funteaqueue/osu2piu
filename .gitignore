out/
__pycache__/
*.pyc
training/

node_modules/
dist/

# app data / local deployment config — see .env.example
.env
web/server/.env.local
projects/
publish/*
!publish/.gitkeep
noteskin/*
!noteskin/.gitkeep
# the pump noteskin sprites ship with the repo so a fresh clone renders the
# real in-game arrows (see .env.example: NOTESKIN_SOURCE_DIR to override)
!noteskin/*.png
